language: r
sudo: false
dist: trusty
group: deprecated-2017Q4

r:
  - oldrel
  - release
  - devel

cache: packages

install:
  - Rscript -e 'install.packages(c("devtools","roxygen2","testthat"));

notifications:
  email:
    on_success: change
    on_failure: change
